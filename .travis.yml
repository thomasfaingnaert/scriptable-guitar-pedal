dist: trusty
sudo: false
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-arm-linux-gnueabihf

script:
    - cmake -DCMAKE_TOOLCHAIN_FILE="cmake/Toolchain.cmake" .
    - cmake --build .
